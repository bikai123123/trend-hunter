name: Daily Trend Update

on:
  schedule:
    - cron: '0 0 * * *' # 每天 UTC 00:00 (北京时间 08:00)
  workflow_dispatch:    # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 【关键变动】安装 Google AI 库
        pip install requests google-generativeai

    - name: Run crawler script
      # 【关键变动】将 GitHub Secret 注入到 Python 环境变量中
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      run: python daily_crawl.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'Trend Bot'
        git config --global user.email 'bot@noreply.github.com'
        git add index.html
        # 防止空提交报错，且每次自动拉取最新代码避免冲突
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔥 AI Auto-update: Daily trends refreshed" && git pull --rebase && git push)
