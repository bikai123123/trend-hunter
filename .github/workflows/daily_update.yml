name: Global Intelligence Update

on:
  schedule:
    # 对应北京/新加坡时间: 07:00, 11:00, 15:00, 19:00, 23:00
    - cron: '0 23,3,7,11,15 * * *'
  workflow_dispatch: # 允许手动立刻刷新

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    - name: Run intelligence crawler
      run: python daily_crawl.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'Intelligence Bot'
        git config --global user.email 'bot@noreply.github.com'
        git add index.html
        # 只有当有新数据写入时才提交
        git diff --quiet && git diff --staged --quiet || (git commit -m "🚀 Auto-update: 5x Daily News Refreshed" && git pull --rebase && git push)
