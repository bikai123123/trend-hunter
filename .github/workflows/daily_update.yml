name: Daily Trend Update

# 触发机制：
# 1. schedule: 每天国际标准时间 00:00 (北京时间早上 8:00) 自动运行
# 2. workflow_dispatch: 允许你手动点击按钮立即运行 (方便测试)
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 赋予机器人修改代码的权限

    steps:
    # 1. 把代码拉取到虚拟服务器
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. 安装 Python 环境
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    # 3. 安装依赖库 (requests)
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests

    # 4. 运行你的爬虫脚本
    - name: Run crawler script
      run: python daily_crawl.py

    # 5. 提交并推送更改 (如果 index.html 变了的话)
    - name: Commit and push changes
      run: |
        git config --global user.name 'Trend Bot'
        git config --global user.email 'bot@noreply.github.com'
        git add index.html
        git commit -m "🔥 Auto-update: Daily trends refreshed" || exit 0
        git push
